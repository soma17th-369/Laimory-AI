---
name: plan-work
description: 여러 단계의 구현·수정 작업을 시작하기 전에 실행계획을 `.agents/plans/`의 Markdown 파일로 저장하고, 사용자 승인을 받은 뒤 계획대로 작업하며, 검증 완료 후 `.agents/worklog/`에 결과를 기록한다. "실행계획 세워줘", "계획부터 잡고 작업해줘", "이 작업 진행해줘" 같은 요청이나 복수 파일·설계·배포·계약 변경처럼 사전 합의가 필요한 작업에 사용한다. 단순 질의, 읽기 전용 조사, 상태 보고, 사소한 단일 변경에는 사용하지 않는다.
---

# plan-work

계획 파일을 구현의 유일한 기준으로 삼아 계획 → 승인 → 작업 → 기록 순서를 지킨다.

## 핵심 규칙

- 모든 계획과 worklog를 한글 중심의 Markdown으로 작성한다.
- 계획이 필요한 작업에서는 관련 파일을 읽고 계획 파일을 만드는 것까지만 먼저 수행한다.
- 사용자가 계획 파일을 확인하고 명시적으로 승인하기 전에는 제품 코드·설정·문서를 수정하지 않는다.
- 최초 작업 요청 자체를 계획 승인으로 간주하지 않는다.
- 승인 후에는 계획 전체를 진행하되, 범위·접근 방식·완료 조건이 실질적으로 바뀌면 계획을 수정하고 다시 승인받는다.
- Markdown 계획 파일을 정본으로 유지한다. 도구의 임시 task/plan 목록은 보조 수단으로만 사용한다.
- 기존 작업 트리 변경을 사용자 작업으로 보고 되돌리지 않는다.
- 커밋·push·PR은 사용자가 별도로 요청한 경우에만 수행한다.

## 1. 계획 파일 만들기

1. 관련 코드, 문서, 테스트와 현재 git 상태를 먼저 확인한다.
2. 같은 작업의 기존 계획이 있으면 새 파일을 만들지 말고 이어서 갱신한다.
3. [plan-template.md](references/plan-template.md)를 따라 다음 경로에 저장한다.

```text
.agents/plans/YYYY-MM-DD[-issue-N]-짧은-슬러그.md
```

4. 최초 상태는 `draft`로 둔다. 계획에는 목표, 범위와 제외 범위, 현재 구조, 단계별 변경, 검증, 위험·결정 사항을 포함한다.
5. 구현 단계는 완료 여부를 갱신할 수 있는 체크박스로 작성한다.

## 2. 승인받기

- 사용자에게 계획 파일 링크와 핵심 단계·결정 사항을 요약해서 보여준다.
- “진행해”, “승인”, “이대로 작업해”처럼 현재 계획을 대상으로 한 명시적 답변을 기다린다.
- 승인받으면 frontmatter의 `status`를 `approved`, `approved_at`을 현재 시각, `approved_by`를 `user`로 갱신한다.
- 수정 요청을 받으면 계획을 고치고 `draft` 상태로 다시 확인받는다.

## 3. 계획대로 작업하기

- 실제 수정에 착수할 때 상태를 `in_progress`로 바꾼다.
- 각 단계를 완료할 때 계획의 체크박스를 갱신하고 필요한 검증을 실행한다.
- 계획 안의 안전하고 가역적인 세부 구현 판단은 자율적으로 진행한다.
- 새 외부 시스템, 파괴적 변경, 공개 계약 변경 등 승인 범위를 넓히는 상황에서는 중단하고 계획을 수정해 재승인받는다.
- 단순 실패나 테스트 오류는 원인을 조사하고 계획 범위 안에서 해결한다.

## 4. 완료하고 worklog 남기기

1. 완료 조건과 검증 결과를 재확인한다.
2. 계획의 체크박스를 마무리하고 `status: completed`, `completed_at`을 기록한다.
3. [worklog-template.md](references/worklog-template.md)를 따라 다음 파일을 작성한다.

```text
.agents/worklog/YYYY-MM-DD[-issue-N]-같은-슬러그.md
```

4. worklog에는 승인된 계획 링크, 실제 변경, 중요한 판단, 검증 결과, 변경 파일, 남은 일을 기록한다.
5. 사용자에게 계획 대비 결과와 검증을 요약하고 worklog 경로를 알린다.

작업을 완료하지 못한 경우 계획 상태를 `blocked` 또는 `cancelled`로 남기고, 완료 worklog를 꾸며 쓰지 않는다.
